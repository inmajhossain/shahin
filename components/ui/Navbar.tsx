"use client";
import{useEffect,useState}from"react";import{AnimatePresence,motion}from"framer-motion";
const links=[["about","About"],["projects","Work"],["experience","Journey"],["contact","Contact"]]as const;
export function Navbar(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>24);fn();addEventListener("scroll",fn,{passive:true});return()=>removeEventListener("scroll",fn)},[]);const go=(id:string)=>{document.getElementById(id)?.scrollIntoView({behavior:"smooth"});setOpen(false)};return <header className={`fixed inset-x-0 top-0 z-50 transition-all ${scrolled?"border-b border-white/10 bg-background/75 backdrop-blur-xl":""}`}><nav className="flex justify-between items-center mx-auto px-5 lg:px-8 max-w-7xl h-18" aria-label="Primary navigation"><button onClick={()=>go("hero")} className="flex items-center gap-3 font-black tracking-tight"><span className="place-items-center grid bg-primary rounded-full size-8 text-black text-sm">I</span><span>INMAJ®</span></button><div className="hidden md:flex items-center gap-8">{links.map(([id,label])=><button key={id} onClick={()=>go(id)} className="text-text-muted hover:text-primary transition eyebrow">{label}</button>)}</div><a href="mailto:developerinmaj@gmail.com" className="hidden md:block px-5 py-2.5 border border-white/15 hover:border-primary rounded-full font-bold hover:text-primary text-sm transition">Start a project ↗</a><button className="md:hidden px-4 py-2 border border-white/15 rounded-full text-sm" onClick={()=>setOpen(!open)} aria-label="Toggle menu" aria-expanded={open}>Menu</button></nav><AnimatePresence>{open&&<motion.div initial={{opacity:0,height:0}} animate={{opacity:1,height:"auto"}} exit={{opacity:0,height:0}} className="md:hidden bg-background px-5 pb-6 border-white/10 border-b">{links.map(([id,label])=><button key={id} onClick={()=>go(id)} className="block py-4 border-white/8 border-b w-full font-bold text-2xl text-left">{label}</button>)}</motion.div>}</AnimatePresence></header>}
