"use client";
import{AnimatePresence,motion}from"framer-motion";import{useState}from"react";
const faqs=[["What kind of projects do you take on?","Product websites, portfolios, landing pages and full-stack web applications where design quality and performance both matter."],["How long does a project take?","A focused landing page usually takes 1–3 weeks. Larger product experiences are scoped after a short discovery call."],["Can we work remotely?","Absolutely. I collaborate remotely with clear milestones, async updates and focused review calls."],["Do you provide ongoing support?","Yes. I can provide launch support, maintenance and iterative improvements based on the needs of your product."]];
export default function FAQSection(){const[active,setActive]=useState<number|null>(0);return <section className="px-5 py-28 lg:px-8 lg:py-40"><div className="mx-auto max-w-5xl"><div className="mb-10"><span className="eyebrow text-primary">Questions, answered</span><h2 className="mt-4 text-4xl font-black tracking-[-.05em] sm:text-6xl">Before we begin.</h2></div><div className="border-t border-white/10">{faqs.map(([q,a],i)=>{const isOpen=active===i;return <div key={q} className="border-b border-white/10"><button onClick={()=>setActive(isOpen?null:i)} className="flex w-full items-center justify-between gap-5 py-7 text-left" aria-expanded={isOpen} aria-controls={`faq-${i}`}><span className="text-lg font-bold sm:text-2xl">{q}</span><span className="text-2xl text-primary">{isOpen?"−":"+"}</span></button><AnimatePresence initial={false}>{isOpen&&<motion.div id={`faq-${i}`} initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden"><p className="max-w-3xl pb-7 leading-relaxed text-text-muted">{a}</p></motion.div>}</AnimatePresence></div>})}</div></div></section>}
